# ml-pipes-vision

Vision payloads and vision-domain operators for `ml-pipes`.

## Public Modules

- `ml_pipes.vision`

## Install

```bash
pip install ml-pipes-vision
pip install 'ml-pipes[vision]'
```

`ml-pipes-vision` depends on `ml-pipes-core` and `ml-pipes-tensor`.
