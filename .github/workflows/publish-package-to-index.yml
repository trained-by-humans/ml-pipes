name: Publish Package

on:
  workflow_call:
    inputs:
      index_url_base:
        description: Base package index URL used for artifact existence checks.
        required: true
        type: string
      repository_url:
        description: Upload endpoint for the selected package index.
        required: true
        type: string
      dist_name:
        description: Canonical distribution name to stage, check, and publish.
        required: true
        type: string
      checkout_ref:
        description: Git ref to check out while staging and publishing artifacts.
        required: false
        type: string
    secrets:
      package_index_api_token:
        description: Upload token for the target package index.
        required: true

jobs:
  publish:
    name: Publish ${{ inputs.dist_name }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.checkout_ref != '' && inputs.checkout_ref || github.ref }}

      - name: Check package index token
        env:
          PACKAGE_INDEX_API_TOKEN: ${{ secrets.package_index_api_token }}
        run: |
          if [ -z "${PACKAGE_INDEX_API_TOKEN}" ]; then
            echo "Missing required package index token for the reusable publish workflow." >&2
            exit 1
          fi

      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-packages
          path: dist/release

      - name: Stage artifacts for distribution
        run: |
          python3 .github/scripts/stage_release_artifacts.py \
            --artifacts-dir dist/release \
            --staging-dir publish \
            --dist-name "${{ inputs.dist_name }}"

      - name: Check staged artifacts against package index
        id: check_index_artifacts
        run: |
          python3 .github/scripts/check_index_artifacts.py \
            --packages-dir publish \
            --dist-name "${{ inputs.dist_name }}" \
            --index-url-base "${{ inputs.index_url_base }}" \
            --github-output "$GITHUB_OUTPUT"

      - name: Publish package
        if: ${{ steps.check_index_artifacts.outputs.artifacts_missing == 'true' }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.package_index_api_token }}
          packages-dir: publish
          repository-url: ${{ inputs.repository_url }}
