[tool.uv.workspace]
members = [
    "packages/core",
    "packages/tensor",
    "packages/vision",
    "packages/onnx",
    "packages/torch",
    "packages/meta",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
